---
type: Fix
scope:
  - ckeditor5-utils
closes:
  - https://github.com/ckeditor/ckeditor5/issues/20237
communityCredits:
  - ld3nl
---

Fixed editor initialization failing with `RangeError: Maximum call stack size exceeded` in Safari 27 on Intel Macs, caused by a JSC miscompile of `String.prototype.substr` with a negative length in `createEventNamespace()`.
